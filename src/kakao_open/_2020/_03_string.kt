package kakao_open._2020

class _03_string {
    companion object {
        @JvmStatic
        fun main(args: Array<String>) {
            var key = arrayOf(intArrayOf(0,0,0), intArrayOf(1,0,0), intArrayOf(0,1,1))
            var lock = arrayOf(intArrayOf(1,1,1), intArrayOf(1,1,0), intArrayOf(1,0,1))
            System.out.println(solution(key, lock))
        }

        fun solution(key: Array<IntArray>, lock: Array<IntArray>): Boolean {
            var answer = true
            return answer
        }
    }
}

